// TODO: Create proper auth schema when form validation is set up
export const loginSchema = {
    // Placeholder for now - will be replaced with proper Zod schema
    // Example:
    // z.object({
    //   id: z.string().min(1, "User ID is required"),
    //   password: z.string().min(6, "Password must be at least 6 characters")
    // })
};
